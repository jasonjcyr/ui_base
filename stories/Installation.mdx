export const meta = {
  title: '📘 Introduction/Installation',
};

# 📦 Install Talon UI

To get started, install the package via npm:

```bash
npm install @reactnextjsguru/talon-ui

📦 View on npm

Talon UI is fully compatible with:
	•	React 19
	•	Next.js App Router
	•	Server Components
	•	Strict TypeScript
```
