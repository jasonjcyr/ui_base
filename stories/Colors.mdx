import './colors.css';
import colors from './colors.json';

import React from 'react';

export const meta = {
  title: 'Design Tokens/Colors',
};

# 🎨 Colors

A live preview of color tokens from your design system. Each swatch is grouped by category (e.g., core, semantic, dark, extended).

{Object.entries(colors).map(([category, colorSet]) => (

<section key={category} className="color-section">
  <h2 className="color-heading">{category.charAt(0).toUpperCase() + category.slice(1)}</h2>
  <div className="color-grid">
    {Object.entries(colorSet).map(([name, value]) => (
      <div key={name} className="color-swatch">
        <div className="color-box" style={{ backgroundColor: value }} />
        <div className="color-meta">
          <div className="color-name">{name}</div>
          <div className="color-value">{value}</div>
        </div>
      </div>
    ))}
  </div>
</section>
))}
